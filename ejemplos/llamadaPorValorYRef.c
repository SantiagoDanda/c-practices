#include <stdio.h>

/*Cuando se pasa una variable a una función por valor, se crea una copia del argumento en la función que
se invocó. Cuando la llamada se pasa por referencia, se permite modificar el valor de la variable real
que se pasa*/

int contador ();

int main()
{

}